import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


export default function InventoryManagement() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View><Text>Inventory Management</Text></View>
    </SafeAreaView>
  );
}